import { average, sum } from './index.js'

let arr = [56, 12, 34, 97]

console.log('marks.js AVERAGE', average(arr))
console.log('marks.js SUM', sum(arr))

/*
	./: Current folder
	../: Parent folder
	/: Child folder
*/

